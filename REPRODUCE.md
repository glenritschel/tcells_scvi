# Reproduce: tcells_scvi (Paper 3)

## 1) Create environment
```bash
micromamba env create -n tcells-scvi -f environment.yml
micromamba activate tcells-scvi
python -m pip install -r requirements-pip.txt

